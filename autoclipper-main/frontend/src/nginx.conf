server {
  listen 80;
  location / {
    root /usr/share/nginx/html;
    try_files $uri $uri/ /index.html;
  }
  # If you proxy API calls to the backend:
  location /api/ {
    proxy_pass http://backend:8000/api/;
  }
}
