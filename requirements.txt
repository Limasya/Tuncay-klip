# ─────────────────────────────────────────────────────────────────────────
# requirements.txt — tam ortam (base + ml). Geriye dönük uyumlu giriş noktası.
# ─────────────────────────────────────────────────────────────────────────
# Bağımlılıklar iki dosyaya ayrıldı:
#   • requirements-base.txt → FastAPI/API katmanı (hafif)
#   • requirements-ml.txt   → torch/tensorflow/mediapipe/... (ağır) + base
#
# `pip install -r requirements.txt` eskisiyle BİREBİR aynı tam seti kurar
# (monolit / CI için). Hafif bir mikroservis kurmak isteyen sadece
# `requirements-base.txt` kurar ve ~10x küçük bir ortam elde eder.

-r requirements-ml.txt
