# ─────────────────────────────────────────────────────────────────────────
# requirements-base.txt — API / web katmanı (hafif bağımlılıklar)
# ─────────────────────────────────────────────────────────────────────────
# torch/tensorflow/mediapipe gibi ağır ML kütüphaneleri İÇERMEZ.
# ML gerektiren servisler için: pip install -r requirements-ml.txt
#   (requirements-ml.txt bu dosyayı zaten -r ile içerir)
# API-only / hafif mikroservisler için: pip install -r requirements-base.txt

# Core Web Framework
# fastapi/starlette pin: httpx>=0.28 requires starlette>=1.0.0, which needs fastapi>=0.139.0
fastapi>=0.139.0
starlette>=1.0.0
uvicorn[standard]>=0.30.1
python-dotenv>=1.0.1
pydantic>=2.7.0
pydantic-settings>=2.3.0
httpx>=0.28.0
aiohttp>=3.9.5
websockets>=12.0

# Message Queue
redis>=5.0.7
celery>=5.4.0

# Database
sqlalchemy>=2.0.31
aiosqlite>=0.22.0
asyncpg>=0.29.0
alembic>=1.13.1

# Video Downloading (hafif yt-dlp/curl — ağır codec yok)
yt-dlp>=2024.1.0
curl_cffi>=0.15.0

# LLM API istemcileri (hafif HTTP SDK — torch çekmez)
groq>=0.9.0
cohere>=5.5.0
together>=1.3.0
openai>=1.35.0
# LiteLLM SDK — çoklu provider tek arayüz (proxy değil, in-process Router)
# Sıfır maliyet: yalnızca local backendler + template fallback etkin.
# Kullanıcı kendi API keyiyle uzak provider'ları açana kadar hiçbir ücretli
# sağlayıcı yüklenmez (bkz. litellm_config.yaml).
litellm>=1.52.0
pyyaml>=6.0.1

# Cloud Storage
boto3>=1.34.131

# Auth & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9

# Utilities
srt>=3.5.3
jinja2>=3.1.4
aiofiles>=24.1.0
psutil>=5.9.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Observability (IP_PART6 — Platform Engineering)
prometheus-client>=0.20.0
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-instrumentation-fastapi>=0.46b0
opentelemetry-exporter-otlp-proto-grpc>=1.25.0
strawberry-graphql[fastapi]>=0.206.0
